-- Intermediate Graphical User Interface Library.

local GUI = {
    background = {
        corner_l = Sprite(),
        corner_r = Sprite(),
        top      = Sprite(),
        bottom   = Sprite(),
    },
    entities = {}
}

function GUI:Begin(id, xy, wh)

end

function GUI:End()

end

return GUI
